[[cron]]
  name = "appointment-reminders"
  schedule = "0 12,0 * * *"  # 8:00 AM y 8:00 PM hora Chile (UTC-4)
  command = "curl -X POST -H 'x-cron-secret: $CRON_SECRET' https://brisaconsciente.cl/api/cron/reminders"
